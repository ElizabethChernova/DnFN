export class Visualizer {
    motionStrategy;
    game;

    constructor(motionStrategy, game) {
        this.motionStrategy = motionStrategy;
        this.game = game;
    }

    drawTo(container) {}
}