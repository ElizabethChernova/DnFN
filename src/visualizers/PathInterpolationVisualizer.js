import { Visualizer } from "./Visualizer.js";

export class PathInterpolationVisualizer extends Visualizer {
    drawTo(container) {
        // TODO: Implement visualizations for path interpolation
        // This method is called before rendering each frame. The container is always empty (no persistence between
        // frames) and can be filled with any primitives necessary for the visualizations.
    }
}